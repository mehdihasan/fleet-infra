apiVersion: v1
kind: Service
metadata:
  name: {{ include "dornocV2.fullname" . }}
  labels:
    {{- include "dornocV2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dornocV2.selectorLabels" . | nindent 4 }}
